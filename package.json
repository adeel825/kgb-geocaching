{
  "name": "kgb-geocaching",
  "version": "0.1.0",
  "description": "Khan Geocaching Bureau — static site for KGB caches",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "rimraf": "^5.0.5"
  }
}
